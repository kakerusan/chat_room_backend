spring:
  application:
    name: chatBackend
  datasource:
    # Docker MySQL 映射 3307（本机 3306 已被占用）；自部署改此处即可
    url: jdbc:mysql://localhost:3307/chatroom_lab?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: chatroom
    password: chatroom123
  servlet:
    multipart:
      max-file-size: 2GB
      max-request-size: 2GB

mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
  type-aliases-package: fun.hatsumi.chatbackend.user.entity,fun.hatsumi.chatbackend.chat.entity,fun.hatsumi.chatbackend.file.entity,fun.hatsumi.chatbackend.network.entity
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      id-type: auto

chatroom:
  jwt:
    issuer: chatroom-lab
    # 生产环境务必用环境变量覆盖 CHATROOM_JWT_SECRET；dev 默认值仅满足 >=32 字节校验
    secret: ${CHATROOM_JWT_SECRET:dev-only-secret-0123456789abcdef0123456789abcdef}
    expire-hours: 12
  storage-root: ./data/storage
  log-root: ./data/logs/network-tests
  error-simulation-rate: 0.20
  error-simulation-seed:
  tcp-port: 9001
  udp-port: 9002
  blocking-benchmark-port: 9101
  nio-benchmark-port: 9102
  ws:
    auth-timeout-seconds: 5
    heartbeat-seconds: 20
    idle-timeout-seconds: 60
    max-text-message-size: 65536
